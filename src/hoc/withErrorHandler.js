import * as utils from '../utilities/utilities';

const WithErrorHandler = () => {
    return () => {

        return (
            <>
                
            </>
        );
    };
};

export default WithErrorHandler;